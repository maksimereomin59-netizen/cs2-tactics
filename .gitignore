# Ничего не игнорируем: anon-ключ Supabase публичен по дизайну
# (доступ к данным закрыт RLS-политиками, PIN проверяется на сервере).
# Секретный service_role key в репозиторий класть НЕЛЬЗЯ — приложение его отклонит.

# Зависимости проверок (tests/) в репозиторий не кладём
node_modules/
